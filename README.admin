``` bash
python3 manage.py createsuperuser
```

$PROTO://$DOMAINn/admin/doc


## Image management

There should be a Kooplex instance with docker image registry running

And all the ${prefix}-notebook-${imagename}-base should be pushed to it
e.g.: 
* docker tag kooplex-fiek-notebook-dataexp-base kooplex-fiek:5000/kooplex-fiek-notebook-dataexp-base
* docker push kooplex-fiek:5000/kooplex-fiek-notebook-dataexp-base

# Then on the other instance it can be pulled by
* docker pull kooplex-fiek:5000/kooplex-fiek-notebook-dataexp-base
* dk tag kooplex-fiek:5000/kooplex-fiek-notebook-dataexp-base kooplex-temp-notebook-dataexp-base

# Then build the top, system specific layer onto it
* bash kooplex.sh build notebook dataexp

